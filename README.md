# FanArt Roller Bot

Telegram-бот для случайной генерации идей по фанфикшену на основе двух пользовательских списков: фандомов и ситуаций.

## Описание

**FanArt Roller Bot** помогает авторам фанфиков генерировать творческие идеи путём случайного выбора фандома из первого списка и ситуации из второго. Бот сохраняет личные списки каждого пользователя в базе данных.

## Функциональность

- 📝 **Управление списками** — добавляйте, удаляйте и просматривайте слова в двух списках:
  - Список A: Фандомы
  - Список B: Ситуации
  
- 🎲 **Ролл** — случайная генерация комбинации из двух списков
  
- 💾 **Персональные данные** — каждый пользователь имеет собственные списки
  
- ⌨️ **Интерактивный интерфейс** — управление через кнопки и текстовый ввод

## Требования

- Python 3.10+
- Telegram Bot Token

## Зависимости

```
aiogram>=3.0.0
aiosqlite>=0.17.0
python-dotenv>=0.19.0
```

## Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/MaksimLevchenko/fanart_roller_bot.git
cd fanart_roller_bot
```

2. **Создайте виртуальное окружение (рекомендуется):**
```bash
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`:**
```
BOT_TOKEN=your_bot_token_here
LOG_LEVEL=INFO
```

Получить токен можно у [@BotFather](https://t.me/botfather) в Telegram.

## Использование

Запустите бота:
```bash
python fan_art_roller.py
```

### Команды бота

- `/start` — начать работу и перейти в главное меню
- `/menu` — вернуться в главное меню

### Основной интерфейс

1. **Редактировать список Фандомов** — управление первым списком
2. **Редактировать список Ситуаций** — управление вторым списком
3. **Ролл** — получить случайную комбинацию

### Добавление слов

При выборе пункта редактирования можно:
- Добавить несколько слов за раз (каждое на новой строке)
- Удалить отдельное слово, выбрав его из списка
- Вернуться в главное меню

## Структура проекта

- `fan_art_roller.py` — основной файл бота со всей логикой
- `lists.db` — база данных (создаётся автоматически при первом запуске)
- `.env` — конфигурация с токеном бота

## База данных

Бот использует SQLite для хранения данных. Таблица `words` содержит:
- `user_id` — ID пользователя Telegram
- `list_name` — имя списка (A или B)
- `word` — сохранённое слово

## Логирование

Уровень логирования настраивается переменной `LOG_LEVEL` в файле `.env` (по умолчанию `INFO`).

## Автор

- **Максим Левченко** (MaksimLevchenko)

## Лицензия

Не указана.

## Поддержка

При возникновении проблем создайте issue в репозитории проекта.
